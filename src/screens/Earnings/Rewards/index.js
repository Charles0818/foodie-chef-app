import * as React from 'react';
import { View, Text } from 'react-native'
import { Screen, Section } from '../../Wrapper';
import { styles } from '../../styles';

export default Rewards = () => {
  return (
    <Screen style={[styles.flexCenter]}>
      <Text>Reward Credits</Text>
    </Screen>
  )
}